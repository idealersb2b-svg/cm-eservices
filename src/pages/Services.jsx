import PagePlaceholder from '../components/PagePlaceholder/PagePlaceholder'

/**
 * Services route.
 * Structure only — real content/sections to be designed next.
 */
export default function Services() {
  return (
    <PagePlaceholder
      eyebrow="What we do"
      title="Services built for scale"
      description="From code to campaigns — everything under one roof."
    />
  )
}
