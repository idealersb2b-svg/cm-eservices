import PagePlaceholder from '../components/PagePlaceholder/PagePlaceholder'

/**
 * AITools route.
 * Structure only — real content/sections to be designed next.
 */
export default function AITools() {
  return (
    <PagePlaceholder
      eyebrow="AI Tools"
      title="Tools we build with AI"
      description="Internal and client-facing AI tooling, engineered for real workflows."
    />
  )
}
